# A4 Document Change Manifest
## Exact Updates Required for Existing NAAS Project Documents

**Purpose:** This document lists the specific sections to update in existing project documents to integrate the ML Model Bootstrap Workflow design decisions from A4.

**Context:** The A4 design specifies how `random_forest.pkl` is generated, defines a 16-column feature vector (broader than the 4-signal rule-based path), introduces a bootstrap training script with 12 distribution profiles, and establishes the feature column ordering contract via a shared module. Several existing documents reference the ML model in ways that are now insufficiently specified or slightly inconsistent with the A4 design.

---

## 1. SYSTEM_ARCHITECTURE.md

### 1a. Replace "ML Training Service" section

**Replace** the current ML Training Service section with:

```markdown
### ML Bootstrap Script (MVP) / ML Training Service (P2)
- **MVP:** Standalone script `scripts/train_bootstrap_model.py` generates synthetic training data from parameterized distribution profiles, trains a Random Forest classifier, evaluates it, and serializes `random_forest.pkl` to `services/risk-evaluator/models/`. Run once during initial setup; the `.pkl` file is committed to the repo for first-boot.
- **Feature vector:** 16 columns derived from the full evaluation context — 4 continuous pre-normalized signals, 4 boolean enrichment signals, 2 derived time features, 3 one-hot protocol columns, 3 one-hot employee_type columns. Column ordering defined in `shared/naas_shared/ml_features.py`.
- **Training data:** 12 distribution profiles (6 benign, 6 malicious) encoding IAM domain knowledge about attack patterns. 70/30 benign-to-malicious class balance. Labels are independent of rule-based scoring — avoids entanglement anti-pattern.
- **P2:** Full ML Training Service — offline retraining from production data, hyperparameter tuning, model versioning, Jupyter notebooks.
- See `A4_ML_Model_Bootstrap_Workflow.md` for full design including feature vector spec, all 12 profiles, sampling algorithm, and evaluation criteria.
```

### 1b. Update §6 (Risk Evaluator Service) — ML-based bullet

In the Risk Evaluator Service section, **replace**:

```markdown
  - **ML-based:** Random Forest classifier (scikit-learn, joblib serialized). Predicts probability of malicious.
```

**with:**

```markdown
  - **ML-based:** Random Forest classifier (scikit-learn, joblib serialized). 16-feature vector extracted from full evaluation context (not limited to the 4 signal_weights signals). Predicts probability of malicious. Falls back to `0.0` (ML path disabled) if model file missing. See `A4_ML_Model_Bootstrap_Workflow.md` for feature vector spec and training workflow.
```

---

## 2. NAAS_System_Decomposition_Guide.md

### 2a. Update Spec 3 — Risk Evaluator Service bullets

In the **Risk Evaluator Service** bullet list, **replace**:

```markdown
  - ML scoring (Random Forest, joblib model loading)
```

**with:**

```markdown
  - ML scoring (Random Forest, joblib model loading, 16-feature vector from full evaluation context — see A4 spec)
  - ML feature extraction (extract_ml_features function, column ordering from shared/naas_shared/ml_features.py)
  - ML graceful degradation (if model file missing, ml_score returns 0.0)
```

### 2b. Add bootstrap script to Spec 3 scope

In the Spec 3 section, **add** after the Risk Evaluator bullets:

```markdown
- **Bootstrap Script:**
  - `scripts/train_bootstrap_model.py` — standalone CLI, not a service
  - Generates synthetic training data from 12 distribution profiles (6 benign, 6 malicious)
  - Trains RandomForestClassifier, evaluates, serializes to `services/risk-evaluator/models/random_forest.pkl`
  - See `A4_ML_Model_Bootstrap_Workflow.md` for full profile definitions and sampling algorithm
```

---

## 3. NAAS_v2.0_Tech_Stack.md

### 3a. Update ML Framework: Scikit-learn section

**Replace** the current scikit-learn section with:

```markdown
### ML Framework: Scikit-learn

- Random Forest classifier for risk scoring (see Risk Evaluator)
- 16-feature vector: 4 continuous signals + 4 booleans + 2 time derivatives + 6 one-hot categorical columns
- Feature column ordering contract: `shared/naas_shared/ml_features.py` (shared between training script and Risk Evaluator)
- Model serialized via joblib (`random_forest.pkl`)
- Fast inference (< 10ms per prediction)
- Pre-trained model shipped with repository (generated by `scripts/train_bootstrap_model.py`)
- Training data: synthetic, generated from 12 IAM domain-knowledge distribution profiles (independent of rule-based scoring — avoids entanglement)
- See `A4_ML_Model_Bootstrap_Workflow.md` for complete feature vector spec, profile definitions, and training workflow
```

---

## 4. CLAUDE.md

### 4a. Update Project Structure

**Add** to the project structure tree, after the `services/` block:

```markdown
├── scripts/
│   └── train_bootstrap_model.py    # ML model bootstrap — generates random_forest.pkl
├── shared/
│   └── naas_shared/
│       ├── ml_features.py          # ML feature column ordering contract
│       └── simulation_tools.py     # Tool definitions for persona-simulator and MCP server
```

### 4b. Add to Key Conventions

**Add:**

```markdown
- **ML Model:** Bootstrap script at `scripts/train_bootstrap_model.py` generates `random_forest.pkl` from synthetic distribution profiles. Feature vector (16 columns) defined in `shared/naas_shared/ml_features.py` — shared between training and inference. Model labels are independent of rule-based scoring.
```

---

## 5. A3_Policy_Expression_Language_and_Scoring_Model.md

### 5a. Update §10 (What This Spec Does NOT Cover) — ML bullet

**Replace:**

```markdown
- **ML model training.** How `random_forest.pkl` is generated is an A4 concern. This spec assumes the model exists and returns a float 0.0–1.0.
```

**with:**

```markdown
- **ML model training.** How `random_forest.pkl` is generated is defined in `A4_ML_Model_Bootstrap_Workflow.md`. The ML model consumes a 16-feature vector (broader than the 4 signal_weights signals) and returns a float 0.0–1.0. The training data labels are independent of rule-based scoring.
```

---

## 6. NAAS_v2.0_Vision_Document.md

### 6a. Update Risk Scoring bullets

In the Risk Scoring section of the architecture diagram area, if there is a bullet mentioning ML, **add** clarifying text:

```markdown
  - ML ensemble (Random Forest, 16-feature vector trained on synthetic IAM distribution profiles)
```

This replaces any existing bullet that just says "ML ensemble (Random Forest)".

---

## Summary of Changes

| Document | Sections Changed | Nature of Change |
|----------|-----------------|------------------|
| SYSTEM_ARCHITECTURE.md | ML Training Service section, §6 Risk Evaluator | Replace placeholder with A4 design reference, add feature vector details |
| System Decomposition Guide | Spec 3 Risk Evaluator bullets | Add ML feature extraction, graceful degradation, bootstrap script scope |
| Tech Stack | Scikit-learn section | Expand with feature vector spec, training data source, A4 reference |
| CLAUDE.md | Project structure, Key conventions | Add scripts/ directory, ML model convention |
| A3 Spec | §10 (Not Covered) | Update cross-reference from placeholder to A4 spec |
| Vision Document | Risk Scoring bullets | Minor clarification of ML feature vector |

---

*End of A4 Change Manifest.*
